{
  "name": "yank",
  "version": "1.0.0",
  "description": "Lightweight way to parse strings (that isn't regex)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
